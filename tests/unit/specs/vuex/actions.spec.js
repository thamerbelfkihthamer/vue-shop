/*
import actions from '../../../../src/vuex/actions'
import { CHANGE_TITLE, POPULATE_SHOPPING_LIST} from "../../../../src/vuex/mutation_types"
import sinon from 'sinon'

describe('actions.js', () => {

    let lists, store

    beforeEach(() => {
        // mock shopping list
        lists = [
            {
                id: 1,
                title: 'Groceries'
            },
            {
                id:	'2',
                title:	'Clothes'
            }
        ]

        // mock store commit method
        store = {
            commit: (method, data) => { },
            state: {
                shoppingList: lists
            }
        }
        sinon.stub(store, 'commit')
    })


    describe('POPULATE_SHOPPING_LIST', () => {

    })

})
*/
